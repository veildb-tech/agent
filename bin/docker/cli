#!/bin/sh

[ -z "$1" ] && echo "Please specify a Console command (ex. ls)" && exit

if [ -z "${PROJECT_ROOT}" ]; then
    export PROJECT_ROOT="$(dirname "$(realpath $0)")/../.."
fi

"${PROJECT_ROOT}"/bin/docker/docker-compose exec -w /app veildb-server "$@"